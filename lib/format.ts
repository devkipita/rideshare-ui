// Re-export from canonical location — use @/lib/formatters for new code
export {
  initials,
  formatDate,
  formatTime,
  formatTimestamp,
  formatDay,
  timeAgo,
  formatPrice,
} from "./formatters";
